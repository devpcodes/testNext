import AccountTable from '../vipInventory/AccountTable';
const VipOrderStatusTable = () => {
    return (
        <>
            <AccountTable columns={[]} dataSource={[]} />
        </>
    );
};

export default VipOrderStatusTable;
